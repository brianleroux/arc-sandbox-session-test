{
  "name": "arc-sandbox-session-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "create": "AWS_PROFILE=default AWS_REGION=us-west-1 arc-create",
    "deploy": "AWS_PROFILE=default AWS_REGION=us-west-1 arc-deploy",
    "start": "AWS_PROFILE=default AWS_REGION=us-west-1 NODE_ENV=testing arc-sandbox"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@architect/workflows": "^2.1.0",
    "bel": "^5.1.5"
  }
}
